# 依赖与构建产物
node_modules/
_site/
.jekyll-cache/
.sass-cache/
.bundle/
# 只忽略顶层 bundler 的 vendor，不要连带吞掉 _sass/vendor（主题构建需要它）
/vendor/
dist/
.astro/

# macOS
.DS_Store

# 桌面端回传的中间文件
Claude outputs/

# jQuery 等原始脚本；_config.yml 已把它排除在构建外，站点只用打包好的 main.min.js
assets/js/vendor/

# 本地素材与草稿（不进公开仓库）
photo.jpg
paper-sources/
PLAN.md
docs/

# LaTeX 编译中间文件
cv/*.aux
cv/*.log
cv/*.out
